name: Deploy CV

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  pages:
    uses: corebound-labs/Commons-CI/.github/workflows/pages-deploy.yml@master
    with:
      build-command: 'npm run build'
      publish-path: 'dist'
      node-version: '22'
